using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerAttack : MonoBehaviour
{
    public GameObject WoodenSword; // stater våbenet. 
    public GameObject IronSword; // andet våben kun aktiv hvis spiller har fået det.

    // Start is called before the first frame update
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void Attack(float damage)
    {
        // Her skal der laves en attack animation og lyd effekt. 
    }
}
